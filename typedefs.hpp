#ifndef LATENCY_TESTRUINO_TYPEDEFS_HPP
#define LATENCY_TESTRUINO_TYPEDEFS_HPP

enum class Jack {
  A,
  B
};

enum class Channel {
  LEFT,
  RIGHT
};

enum class Direction {
  RX,
  TX
};

enum class Button {
  STARTSTOP,
  RESET
};

#endif // LATENCY_TESTRUINO_TYPEDEFS_HPP
